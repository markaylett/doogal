Export one or more documents to the outgoing directory:
${DOOGAL_REPO}/outgoing

The outgoing directory may be changed using the 'set' command.

The exported documents will be converted to HTML.  If the Content-Type is
"text/apt", then the content will be treated as Wiki markup.
